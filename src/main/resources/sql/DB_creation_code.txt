-- DEPRECATED: исторический черновик первоначальной схемы БД.
-- Актуальные миграции живут в src/main/resources/db/changelog/db.changelog-master.xml
-- и применяются Liquibase при старте приложения.
-- Файл оставлен для справки.

create table users(
                      id int generated by default as identity primary key,
                      username varchar(30) not null unique,
                      password varchar(100) not null,
                      email varchar(100),
                      phone_number varchar(15),
                      role varchar(30) not null
);

insert into users(username, password, email, phone_number, role)
values ('test1', 'testpass', 'test@example.com', '+10000000000', 'ROLE_ADMIN');
